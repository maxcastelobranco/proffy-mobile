import { BoxProps } from "@shopify/restyle";

import { Theme } from "../../../../../../../theme";

export const extraContainerStyles: BoxProps<Theme> = {
  borderRadius: "default",
  marginVertical: "xs",
};
